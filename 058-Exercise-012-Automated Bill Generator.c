#include <stdio.h>
#include <string.h>

char *replace(const char *str,const char *oldword,const char *newword){
    char * resultstring;
}

int main()
{
    FILE *ptr = NULL;
    ptr = fopen("058-Bill.txt","r");
    char str[200];
    fscanf(ptr,"%s",str);
    return 0;
}