#include <stdio.h>

int Plaindrom(int a){
    
}

int main()
{
    int number;
    printf("Enter the Number :");
    scanf("%d",&number);
    if (palindrom(number))
    {
        printf("The Number is Plaindrom");
    }
    else{
        printf("The Number is Not Palindrom");
    }
    return 0;
}